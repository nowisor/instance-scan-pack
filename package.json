{
  "name": "nowisor-instance-scan-pack",
  "version": "1.0.0",
  "description": "nowisor Instance Scan Agent — open-source ServiceNow security check pack. Findings flow to the nowisor advisor at nowisor.com via the scan_finding v1 schema.",
  "license": "Apache-2.0",
  "imports": {
    "#now:*": "./@types/servicenow/fluent/*/index.js"
  },
  "scripts": {
    "build": "now-sdk build",
    "deploy": "now-sdk install",
    "transform": "now-sdk transform",
    "types": "now-sdk dependencies"
  },
  "devDependencies": {
    "@servicenow/sdk": "4.6.0",
    "@servicenow/glide": "27.0.5",
    "typescript": "5.5.4"
  }
}